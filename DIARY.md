# D3WebGPU — Porting Diary

A running log of our experience porting the Doom 3 engine (id Tech 4) to the
web. We're documenting every turn: what we tried, what broke, and how we fixed
it — problems and solutions — so we can turn this into a blog post at the end.

## Project context

- **Source**: d3wasm — gabrielcuvillier's port of Doom 3 (id Tech 4) to
  WebAssembly + WebGL.
- **Goal**: get it running locally at http://localhost:3000, then (later) port
  the renderer from WebGL to WebGPU.
- **Machine**: macOS, Apple Silicon (arm64). Homebrew available.

---

## Turn 1 — "Can we run this at localhost:3000?"

**Goal**: Build the engine and serve it locally.

### Reconnaissance
- The repo is NOT a Node/npm project — no package.json at root. It's a C++
  codebase under neo/ built with CMake + Emscripten (compiles to
  .wasm / .js / .html).
- README.md + BUILD.md describe the pipeline: install Emscripten SDK, then
  emcmake cmake, then emmake make, then package game data, then run an HTTP
  server.
- PLAN.md describes an aspirational WebGL to WebGPU port (not started yet;
  user clarified to ignore it for now).
- System check: emcc / emcmake / cmake MISSING; python3 / node present;
  Homebrew present; no ~/emsdk; no build dirs.

### Problem 1 — Toolchain not installed
**Solution**: Installed CMake via Homebrew (brew install cmake -> 4.3.4) and
the Emscripten SDK by cloning emsdk into ~/emsdk and running
./emsdk install latest-upstream + activate -> Emscripten 6.0.2.

### Problem 2 — CMake 4.x rejects the old cmake_minimum_required
Configuring failed with: "Compatibility with CMake < 3.5 has been removed."
The top of neo/CMakeLists.txt had cmake_minimum_required(VERSION 3.0.0 ...) and
project() was called before it (also a CMake warning).
**Solution**: Edited neo/CMakeLists.txt — bumped to
cmake_minimum_required(VERSION 3.5) and moved it above project(d3wasm).

### Build
Re-ran configure, then emmake make -j8. Compiled the whole engine to wasm
(only warnings, no errors). Output in build-wasm/:
- d3wasm.html (11 KB) — the shell page
- d3wasm.js (241 KB) — engine glue
- d3wasm.wasm (3.7 MB) — the compiled engine

### Serving
Started python3 -m http.server 3000 from build-wasm/. Verified with curl:
d3wasm.html / .js / .wasm all return HTTP 200; demo00.js returns 404.

### Problem 3 — No game data (the engine needs assets to actually play)
The page loads but the game cannot start without demo00.pk4 (the Doom 3 Demo's
assets). It's copyrighted, so it isn't shipped in the repo. The build's pre.js
loads demo00.js (generated by the package_demo_data CMake target from
demo00.pk4); without it the engine has no maps/textures/models.
**Solution (partial)**: Created tools/add_game_data.sh so that once we have
demo00.pk4, packaging it is one command. Still need to obtain the pk4.

**Status after Turn 1**: Engine builds and serves at localhost:3000, but not
playable yet — blocked on game data.

---

## Turn 2 — "I added D3Demo.exe, can you get the pk4 from it?"

**Goal**: Extract demo00.pk4 from the official Doom 3 Demo installer.

### Reconnaissance
- User dropped D3Demo.exe into the repo: 461 MB, PE32 executable (GUI) Intel
  80386, for MS Windows — a Windows self-extracting installer.
- No extraction tools present (7z, unar, cabextract, innoextract all missing).

### Problem 4 — How to unpack a Windows installer on macOS?
**Solution (in progress)**: Installed extractors:
- brew install 7zip -> 7zz (7-Zip 26.02)
- brew install unar -> unar / lsar (The Unarchiver 1.10.8)

### Inspecting the installer
7zz l D3Demo.exe revealed the structure:
- An outer ZIP at byte offset ~3.76 MB containing video/idlogo.roq (12 MB) —
  the bootstrapper's intro video.
- A huge 469 MB tail after the zip — this is the real installer payload.
- Math checks out: zip end ~14.4 MB; tail (469 MB) ends exactly at the file
  size (461 MB).

Signature scan of the tail found ISCG (InstallShield Cabinet) markers, and the
tail's leading bytes look encrypted/random. lsar reports "Couldn't recognize
the archive format."

**Diagnosis**: This is an InstallShield self-extracting installer
(PackageForTheWeb-style). The payload is in InstallShield's proprietary cabinet
format, possibly XOR-encrypted, so a plain unzip won't reach demo00.pk4.

### Next (Turn 3)
Need a tool that understands InstallShield cabinets (unshield) or run the
installer under Wine to extract files to disk, then grab demo00.pk4 and feed it
to tools/add_game_data.sh.

**Status after Turn 2**: Identified the installer format; extraction pending.
The demo00.pk4 (itself a renamed ZIP) is almost certainly the bulk of the
469 MB payload — we just need the right extractor.
