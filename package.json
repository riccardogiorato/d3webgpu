{
  "name": "d3webgpu",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "cd build-wasm && python3 -m http.server 3001",
    "start": "cd build-wasm && python3 -m http.server 3001",
    "preview": "cd build-wasm && python3 -m http.server 3001",
    "build": "bash scripts/build.sh",
    "build:wasm": "bash scripts/build.sh d3wasm"
  }
}
