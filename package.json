{
  "name": "d3webgpu",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "dev:ui": "npm --prefix web run dev",
    "dev:all": "node scripts/dev-all.mjs",
    "build": "bash scripts/build.sh && npm --prefix web run build",
    "build:engine": "bash scripts/build.sh",
    "build:ui": "npm --prefix web run build"
  }
}
